= 信息检索

== 从应用场景理解信息检索

信息检索(information retrieval, IR)最直观的场景是:  
给定一个很大的文本库, 其中保存了大量文本, 例如新闻报道、网页、论文、图书记录等.  
用户输入一个查询条件, 系统需要快速、准确地从文本库中找出与该查询相关的文本.

这类问题看似简单, 但实际包含几个关键困难:

- 文本库规模很大, 不能靠人工逐篇阅读.
- 用户查询通常很短, 而文档内容很长.
- 查询词和文档词未必完全相同, 例如“电脑”和“计算机”.
- 用户真正需要的是“相关内容”, 而不是简单包含某个字面词的内容.
- 返回结果还要排序, 因为用户通常只看靠前的少量结果.

因此, 信息检索系统的目标可以概括为:

给定文档库$D$和用户查询$q$,  
系统返回一个按相关性排序的文档列表
$
  [d_1, d_2, dots]
$
其中越靠前的文档应越能满足用户的信息需求.

=== 一个基本流程

信息检索过程拆成四个环节:

1. 文本预处理.
2. 文本标引.
3. 相似度评估.
4. 排序返回.

可以把它理解为一条流水线:

- 先把原始文本清洗成更适合计算的形式.
- 再把文档和查询都转成统一的数学表示.
- 然后计算查询和每篇候选文档之间的相关度.
- 最后按相关度从高到低返回前若干篇文档.

信息检索的核心不只是“找包含查询词的文档”,  
而是要在文档表达、查询表达和相关度计算之间建立一个可计算的模型.

=== 文本预处理

对中文文本来说, 预处理通常包括:

- 中文分词.
- 词性标注.
- 虚词或停用词过滤.

例如句子:

```text
我们在上自然语言处理课程。
```

经过分词后可以得到:

```text
我们 / 在 / 上 / 自然语言处理 / 课程 / 。
```

再结合词性标注和过滤策略, 可以去除一部分对主题区分作用较弱的词.  
课件中的例子强调: 可去除掉动词、名词、副词、形容词之外的其他所有词.  
实际系统中是否保留某类词, 取决于任务目标和模型设计.

文本预处理的意义在于降低噪声, 让后续的标引和相似度计算集中在更有区分力的词项上.

=== 文本标引

标引(indexing)是把文档或查询转换成可计算表示的过程.  
最常见的做法是把文本表示为词项(term)及其权重组成的向量.

一般要解决三个问题:

- 特征选择: 哪些词项可以进入特征集.
- 权重计算: 每个词项在当前文档中有多重要.
- 向量编码: 如何把词项及权重组织成向量.

一种简单的特征选择方法是基于频率:  
统计词项在文档库中出现的频次, 对全部词项排序, 选择频次达到一定阈值的词项作为特征集.

但单纯频率并不总是可靠.  
例如“的”“是”“在”这类词频很高, 却往往不能代表文档主题.  
因此信息检索中常用 TF-IDF 来综合刻画词项的重要性.

=== TF-IDF 权重

TF-IDF(term frequency-inverse document frequency)是一种常用加权方法.  
它同时考虑两个因素:

- TF: 词项在当前文档中出现得越多, 对该文档越重要.
- IDF: 词项在整个文档集合中出现得越少, 区分能力越强.

设$t$为词项, $d$为文档, $D$为文档集合.  
常见写法是:
$
  "tf idf"(t, d)
  = "tf"(t, d) times "idf"(t)
$
其中:
$
  "idf"(t) = log(N / "df"(t))
$

$N$表示文档总数, $"df"(t)$表示包含词项$t$的文档数.  
如果某个词在当前文档中出现很多, 但在几乎所有文档中都出现,  
那么它的 TF 高而 IDF 低, 总权重不会太大.  
如果某个词既在当前文档中频繁出现, 又只出现在少量文档里,  
它就更可能是该文档的主题性词项.

=== 向量空间表示

完成词项选择和权重计算后, 每篇文档都可以表示为一个向量:
$
  d = (w_1, w_2, dots, w_n)
$
其中$w_i$表示第$i$个词项在文档中的权重.

查询也可以用同样方式表示:
$
  q = (v_1, v_2, dots, v_n)
$

这样, 文档检索问题就转化成向量相似度计算问题.  
查询向量和文档向量越接近, 文档就越可能相关.

常用的相似度包括:

- 内积: 直接计算两个向量对应维度乘积之和.
- 余弦相似度: 关注两个向量方向是否接近.
- Jaccard 系数: 更常用于集合形式的特征表示.

余弦相似度是 VSM 中最常见的选择:
$
  "cos"(d, q)
  = (sum_(i=1)^n w_(i,d) w_(i,q))
    / (
      sqrt(sum_(i=1)^n w_(i,d)^2)
      sqrt(sum_(i=1)^n w_(i,q)^2)
    )
$

它的值越接近$1$, 表示两个向量方向越接近, 文档和查询越相似.

== 信息检索概述

=== 产生背景

信息检索技术的出现和发展, 主要来自三方面需求:

- 信息过载: 文档、网页、音视频等数据规模快速增长.
- 需求更高: 用户希望快速找到真正有用的信息, 而不是只得到大量候选结果.
- 用户下沉: 检索系统面向普通用户, 查询表达通常不专业、不规范.

因此, 信息检索不是简单的数据查询.  
它面对的是大规模、非结构化或半结构化内容, 以及不够精确的用户需求表达.

=== 定义

信息检索译自 Information Retrieval, 简称 IR.  
定义可以概括为:

信息检索是从大规模非结构化数据集合中找出满足用户信息需求资料的过程,  
这些数据通常以文本形式存在.

形式化地说:

- 给定一个文档库$D$.
- 用户输入一个表达信息需求的查询$q$.
- 系统返回一个包含所需信息的文档列表$[d_1, d_2, dots]$.

这里有两个关键词:

- 满足信息需求: 关注语义层面的相关性.
- 文档列表: 通常不是单一答案, 而是多个候选文档的排序结果.

=== 应用

IR 是很多系统的底层技术, 典型应用包括:

- 搜索引擎.
- 情报跟踪.
- 内容安全.
- 舆情分析.
- 文献检索.
- 问答系统.

从 NLP 的角度看, 信息检索承担了“从大规模文本中定位相关证据”的功能.  
很多下游任务, 例如问答、摘要和知识发现, 都需要先检索到相关材料.

=== 一般框架

一个信息检索系统通常包含:

- 文档库.
- 用户查询.
- 文档表达.
- 查询表达.
- 相关度评估.
- 文档排序.
- 相关文档列表.

其中最核心的三个环节是:

1. Indexing: 建立文档和查询的统一数学表示.
2. Relevance Measuring: 计算查询表达和候选文档表达之间的相关度.
3. Relevance Sorting: 基于相关度等指标对候选文档排序.

这三个环节分别回答:

- 如何表示.
- 如何比较.
- 如何返回.

因此, 信息检索模型的差异, 本质上就是这些环节的建模方式不同.

=== 好的检索系统要解决的问题

课件列出了三个典型问题:

- 检索出来的内容可能和用户想要的资料毫不相关.
- 用户需要的内容可能被排在很靠后的位置.
- 排在靠前位置的内容可能高度雷同.

第一个问题对应“相关性”,  
第二个问题对应“排序质量”,  
第三个问题对应“多样性”.  
现代搜索系统通常要同时优化这几个目标.

== 信息检索基本模型

信息检索模型是信息检索系统中核心逻辑、规则和算法的统称.  
重点介绍四类基本模型:

- 布尔模型.
- 向量空间模型.
- 语言模型.
- 概率模型.

这些模型都在解决同一个问题:  
如何判断一篇文档是否满足一个查询, 以及应把它排在什么位置.

== 布尔模型

=== 基本思想

布尔模型(Boolean model)是最早的 IR 模型之一, 也是应用非常广泛的模型.  
它把文档表示为关键词集合, 把查询表示为关键词之间的布尔组合.

常见布尔操作包括:

- AND: 与, 要求条件同时满足.
- OR: 或, 满足任一条件即可.
- NOT: 非, 排除某类文档.

文档是否被返回, 取决于它是否满足布尔查询式.  
因此布尔模型的相关度评估本质上是二值匹配:

- 满足查询式: 返回.
- 不满足查询式: 不返回.

=== 技术框架

布尔模型可以概括为:

- 文档标引: 每个文档表示为关键词集合.
- 查询式标引: 查询表示为关键词的布尔组合.
- 相关度评估: 判断文档是否满足查询式.
- 文档排序: 本质上不排序, 所有匹配文档地位相同.

例如查询:

```text
病毒 AND (计算机 OR 电脑) AND NOT 医
```

对三个文档:

```text
D1: 据报道计算机病毒最近猖獗
D2: 小王虽然是学医的, 但对研究电脑病毒也感兴趣
D3: 计算机程序发现了艾滋病病毒传播途径
```

按照布尔条件:

- D1 同时包含“病毒”和“计算机”, 且不包含“医”, 因而会被检索到.
- D2 虽包含“电脑”和“病毒”, 但也包含“医”, 被 NOT 条件排除.
- D3 包含“计算机”和“病毒”, 且不含“医”, 形式上也可能被匹配.

这个例子说明, 布尔模型非常依赖字面条件, 并不真正理解“计算机病毒”和“艾滋病病毒”的语义差异.

=== 优点

布尔模型的优点包括:

- 查询式简单, 普通用户容易理解基本用法.
- 计算效率高, 适合大规模文档集合.
- 可扩展性较好, 专业用户能写复杂布尔表达式实现精确检索.
- 工程实现成熟, 很多检索系统仍支持布尔查询.

例如 Lucene 这类开源检索系统就支持大量布尔式查询机制.

=== 缺点

布尔模型的主要问题是:

- 不支持部分匹配, 要么满足, 要么不满足.
- 很难对输出结果进行细粒度排序.
- 不考虑关键词权重, 出现一次和出现多次的关键词影响相同.
- 对同义词、多义词和语义相关性处理较弱.

因此, 布尔模型适合精确过滤,  
但不适合作为唯一机制解决排序型信息检索问题.

== 向量空间模型

=== 基本定义

向量空间模型(Vector Space Model, VSM)由 Gerald Salton 等人在 20 世纪 70 年代提出,  
并成功应用于 SMART 文本检索系统.

它的核心思想是:

把文本内容简化为高维线性空间中的向量.

在 VSM 中:

- 每个维度对应一个索引项.
- 每篇文档对应一个权重向量.
- 查询也被表示为同一空间中的向量.
- 文档和查询的相关度由向量相似度给出.

=== 基本概念

VSM 中有几个核心概念:

- 文档$D$: 泛指一篇文档或文档中的一个片段.
- 索引项$t$: 能代表文档内容特点的基本语言单位, 如字、词、短语、同义词集或语义概念.
- 索引项权重$w_(t,k)$: 词项$t_k$代表文档$D$的能力大小.
- 相似度$S$: 两个文档或文档与查询之间内容相关程度的量化值.

一篇文档可以表示为:
$
  d_j = (w_(1,j), w_(2,j), dots, w_(n,j))
$

其中$n$是索引项总数, $w_(i,j)$表示第$i$个索引项在第$j$篇文档中的权重.

=== 三个关键点

VSM 的效果主要取决于三个关键点:

1. 索引项选择: 怎样确定文档中哪些词项是重要词项.
2. 权重计算: 怎样确定词项在某篇文档中的重要程度.
3. 相似度评价: 怎样确定文档与查询之间的相似度.

它们分别对应“特征是什么”“特征有多重要”“两个对象有多像”.

=== 索引项选择

索引项选择可以考虑:

- 高频出现的词项.
- 与领域相关的词项.
- 经过归并后的同义或近义词项.

例如在科技文献检索中, “计算机”和“电脑”可能应归并为同一个索引项.  
如果不归并, 用户查询“电脑”时可能漏掉只写“计算机”的文档.

但索引项归并并不容易, 因为它要求系统知道词义关系.  
这也为后面的 LSI 模型埋下伏笔:  
能否不完全依赖人工词典, 而是从“词项-文档”矩阵中自动学习潜在语义结构.

=== 索引项权重

VSM 中最常用的权重计算就是 TF-IDF.  
直观理解是:

- 在当前文档中出现频率越高, 越能代表该文档.
- 在所有文档中越少见, 越有区分能力.

因此:
$
  w_(t,d) = "tf"(t,d) times log(N / "df"(t))
$

在实际系统中还会加入平滑、归一化和长度校正,  
避免长文档因为词多而天然获得更高分.

=== 相似度评价

常见相似度有三类:

1. 内积
$
  "score"(d, q) = d dot q
$

2. 余弦相似度
$
  "cos"(d, q)
  = (d dot q) / (sqrt(d dot d) sqrt(q dot q))
$

3. Jaccard 系数
$
  J(A, B) = (|A inter B|) / (|A union B|)
$

在词项权重向量中, 余弦相似度最常见,  
因为它能弱化文档长度差异, 更关注向量方向.

=== VSM 的优点

和布尔模型相比, VSM 的优点包括:

- 查询式更接近日常自然语言, 不要求用户写复杂布尔条件.
- 支持部分匹配, 查询条件更自由.
- 利用词项权重刻画文档, 文档表示更细致.
- 能根据词项分布特征度量文档和查询的相似度.
- 可以按相似度对返回结果排序.

因此, VSM 更适合大多数面向普通用户的搜索场景.

=== VSM 的不足

VSM 的基本假设也有明显缺陷:

- 索引项之间被认为相互独立, 这不符合语言事实.
- 一词多义会引入错误匹配.
- 一义多词会造成相关文档遗漏.
- 向量空间维度很高, 可能存在噪声和稀疏问题.

例如“处理”既可以表示“处理自然语言”, 也可以表示“处理旧家具”或“处理叛徒”.  
如果只看词项共现, 系统很难区分这些语义.

== 基于语言模型的信息检索

=== 语言模型回顾

语言模型(language model)用于刻画真实语言中词与词之间的联系规律.  
它可以估计一个词序列在真实语言世界中出现的可能性.

在信息检索中, 可以换一个角度理解:

每篇文档都可以看作一个能够生成词语的概率模型.

如果某篇文档的语言模型很可能生成用户查询,  
那么这篇文档就可能与查询相关.

=== 基本流程

基于语言模型的信息检索通常包括:

1. 为每个文档建立文档语言模型.
2. 把查询看作由某个文档语言模型抽样生成的样本.
3. 计算每个文档模型生成查询的概率.
4. 按概率大小对文档排序.

设文档$d$对应语言模型$M_d$, 查询为
$
  q = (t_1, t_2, dots, t_m)
$
若采用一元语言模型, 可写为:
$
  P(q | M_d) = product_(i=1)^m P(t_i | M_d)
$

概率越高, 表示文档$d$越可能生成该查询, 因而越应靠前返回.

=== 一个例子

假设只有两个文本, 分别构造一元语言模型$"LM"_1$和$"LM"_2$.

若查询为:

```text
abacaad
```

则可分别计算:
$
  P(q | "LM"_1)
  = P(a|"LM"_1) P(b|"LM"_1) dots P(d|"LM"_1)
$
$
  P(q | "LM"_2)
  = P(a|"LM"_2) P(b|"LM"_2) dots P(d|"LM"_2)
$

课件中的计算结果是第二个概率更高,  
因此系统返回文本 2 作为检索结果.

语言模型方法的关键点在于:  
排序依据从“向量距离”转成了“生成查询的概率”.

=== 平滑问题

和前面语言模型章节一样, 检索中的语言模型也会遇到零概率问题.  
如果查询中某个词没有在文档$d$中出现, 直接使用 MLE 会导致
$
  P(q | M_d) = 0
$

但这并不一定说明文档无关.  
因此实际系统必须使用平滑, 例如把文档模型和全局集合模型混合:
$
  P(t | M_d)
  = lambda P_("MLE")(t | d)
    + (1-lambda) P(t | C)
$
其中$C$表示整个文档集合.

这可以看作在“当前文档证据”和“全局语言背景”之间做折中.

== 概率模型

=== 基本思想

概率模型直接把“文档是否与查询相关”看成概率判断问题.

给定查询$Q$和候选文档$D$,  
设$R$表示$D$与$Q$相关, $bar(R)$表示不相关.  
模型希望估计:
$
  P(R | D, Q)
$
和
$
  P(bar(R) | D, Q)
$

如果相关概率更高, 则应返回该文档;  
排序时也可以按相关概率从高到低排列.

=== 二值独立模型

经典概率检索模型常使用二值独立假设.  
对每个词项$t_i$, 只关心它是否出现在文档中,  
并假设不同词项在给定相关性条件下相互独立.

训练集中可统计:

- $N$: 文档总数.
- $R_i$: 与查询相关的文档数.
- $n_i$: 包含词项$t_i$的文档数.
- $r_i$: 相关文档中包含词项$t_i$的文档数.

于是可以估计:

- 相关文档中出现$t_i$的概率.
- 不相关文档中出现$t_i$的概率.

最终文档得分可以理解为多个词项证据的累积.  
若某个词项更常出现在相关文档中, 而较少出现在不相关文档中,  
它就应给文档相关性带来更强正向贡献.

=== 与其他模型的关系

概率模型的优势是目标直接: 它直接建模“相关/不相关”.  
但它通常需要相关性标注或反馈信息来估计参数.  
在没有足够训练数据时, 参数估计会比较困难.

可以粗略对比:

- 布尔模型强调规则匹配.
- VSM 强调向量相似度.
- 语言模型强调生成查询的概率.
- 概率模型强调文档与查询相关的概率.

这些模型在现代检索系统中并非互斥,  
实际系统常会把多种得分特征组合起来使用.

== LSI 模型

=== 问题的引出

VSM 的一个关键问题是只在词项表层上建模.  
自然语言中存在:

- 一词多义(polysemy).
- 一义多词(synonymy).

一词多义会引入不需要的结果.  
例如检索词为“处理”时, 可能匹配到:

- 什么地方处理旧家具?
- 你去把那个叛徒处理了.
- 处理自然语言很难.

这些句子都包含“处理”, 但语义差异很大.

一义多词则会遗漏本来相关的结果.  
例如“互联网”“万维网”“因特网”在很多场景中语义接近,  
但如果只按字面词项匹配, 查询其中一个词时可能漏掉另外两种表达.

LSI(latent semantic indexing, 隐性语义索引)试图缓解这些问题.

=== 基本直觉

LSI 的直觉是:

虽然我们看到的是词项和文档的表层共现矩阵,  
但这些表层现象背后可能存在更少量、更本质的语义维度.

例如若干文档同时出现“互联网”“网速”“路由”“冲浪”等词,  
系统可以推测它们可能属于某个网络相关语义维度.  
即使某两篇文档没有共享完全相同的词,  
只要它们在潜在语义维度上接近, 也可以被认为相关.

因此 LSI 要解决的问题是:

- 如何自动得到潜在语义维度.
- 如何建立文档和语义维度之间的关系.
- 如何建立词项和语义维度之间的关系.

如果完全依赖人工语言学知识来定义这些语义维度, 工作量太大.  
因此 LSI 使用矩阵分解方法从大量“词项-文档”关系中自动学习.

=== 词项-文档矩阵

设原始矩阵$C$为“词项-文档”矩阵:

- 行对应词项.
- 列对应文档.
- 元素表示某词项在某文档中的出现情况或权重.

若有$m$个词项、$n$篇文档, 则:
$
  C in R^(m times n)
$

在基本 VSM 中, 每一列就是一篇文档的词项向量.  
LSI 则进一步对这个矩阵做奇异值分解.

=== 奇异值分解

奇异值分解(Singular Value Decomposition, SVD)把矩阵分解为:
$
  C = U Sigma V^T
$

其中:

- $C$: 原始词项-文档矩阵.
- $U$: 词项矩阵.
- $Sigma$: 奇异值矩阵.
- $V^T$: 文档矩阵.

经过 SVD 分解后:

- $V^T$可以理解为用潜在语义维度对文档进行标引.
- $U$可以理解为词项与潜在语义维度之间的关联.
- $Sigma$中的奇异值表示各语义维度的重要性.

严格地说, “语义维度”是为了提高解释性而加上的理解方式.  
SVD 本身只是数学分解, 但分解结果常能捕捉到有用的主题或语义结构.

=== 矩阵含义

1. $C$矩阵

$C$是分解前的原始词项-文档矩阵.  
它的行数等于词表长度, 列数等于文档数量.  
每个元素表示某词项和某文档之间的表层关联.

2. $U$矩阵

$U$矩阵的行数与原始词项数相等.  
从行向量角度看, 每行对应一个词项.  
从列向量角度看, 每列可解释为一个潜在语义维度.

矩阵元素$u_(i,j)$表示:

第$i$个词项与第$j$个语义维度之间的关系强弱.

因此, 每个词项都可以被表示成潜在语义空间中的向量.

3. $V^T$矩阵

$V^T$矩阵的列数与原始文档数相等.  
从列向量角度看, 每列对应一篇文档.  
从行向量角度看, 每行可解释为一个潜在语义维度.

矩阵元素$v_(i,j)$表示:

第$j$篇文档与第$i$个语义维度之间的关系强弱.

因此, 每篇文档也可以被表示成潜在语义空间中的向量.

4. $Sigma$矩阵

$Sigma$是对角矩阵.  
对角线上的元素称为奇异值, 并按从大到小排列.  
奇异值越大, 对应语义维度对重构原始矩阵的贡献越大.

如果把较小的奇异值置为$0$,  
就可以忽略影响较弱的语义维度, 实现降维和噪声过滤.

=== 截断 SVD

在实际使用中, 往往只保留前$k$个最大的奇异值:
$
  C approx C_k = U_k Sigma_k V_k^T
$

这称为截断奇异值分解(truncated SVD).  
它的作用包括:

- 降低维度.
- 去除噪声.
- 用较少潜在语义维度近似原始词项-文档矩阵.

基于 LSI 的文档标引大致步骤是:

1. 对词项-文档矩阵进行 SVD 分解.
2. 保留前$k$个主要语义维度, 得到低维文档表示.
3. 将查询映射到同一低维空间.
4. 计算查询和各文档低维表示之间的相似度.
5. 按相似度输出文档结果.

=== LSI 的优势

LSI 相对基本 VSM 的优势主要有三点.

1. 过滤噪声

较小奇异值通常对应较弱或较细碎的变化.  
将这些奇异值置为$0$, 可以忽略次要语义维度, 保留主要结构.

课件用图像例子说明:  
若两张花朵图像只在颜色上存在枝节差异,  
过滤颜色相关维度后, 更容易看到它们在“花朵”这一主要内容上的相似性.

2. 缓解一义多词

如果“计算机”和“电脑”经常出现在相似文档环境中,  
SVD 可能把它们映射到接近的潜在语义维度上.  
这样, 即使两篇文档表层词不同, 也可能在语义空间中高度相似.

3. 缓解一词多义

多义词通常和多种不同语义维度都有联系.  
在矩阵分解过程中, 它对某个单一语义维度的定义贡献可能被削弱,  
从而降低多义词对检索结果的干扰.

=== LSI 的局限

LSI 虽然能缓解 VSM 的部分问题, 但也有代价:

- SVD 计算成本较高.
- 潜在语义维度不一定容易解释.
- 新文档或新词加入后, 矩阵更新不如传统倒排索引方便.
- 它仍主要基于线性代数结构, 对复杂语义和上下文变化表达有限.

因此 LSI 更像是从词项向量到低维语义表示的一次重要过渡,  
而不是现代语义检索的终点.

== 信息检索系统评测

评测的目标是判断一个检索系统返回的结果是否满足用户需求.  
与分类任务不同, 检索任务往往关心“排序列表”的质量.

=== Precision at N

`P@N`表示前$N$个返回结果中的正确率:
$
  "P@N" = ("前 N 个结果中的相关文档数") / N
$

对于搜索引擎, 大量用户只关注前一两页结果,  
因此`P@10`、`P@20`往往比全列表指标更符合使用习惯.

=== R-Precision

设某个查询对应的相关文档总数为$R$.  
R-Precision 计算检索结果前$R$篇文档中相关文档所占比例:
$
  "R-Precision"
  = ("前"R"篇中的相关文档数") / R
$

它把评价截断点设为该查询的真实相关文档数,  
因此不同查询会有不同的评价深度.

=== Precision-Recall 曲线

召回率(recall)表示系统找回了多少相关文档:
$
  "Recall"
  = ("检索出的相关文档数") / ("全部相关文档数")
$

正确率(precision)表示返回结果中有多少是相关的:
$
  "Precision"
  = ("检索出的相关文档数") / ("检索出的文档数")
$

Precision-Recall 曲线通常在召回率为
$
  0%, 10%, 20%, dots, 100%
$
等位置计算对应正确率, 再绘制曲线.

一般来说:

- 召回率提高时, 系统需要返回更多文档.
- 返回更多文档后, 正确率往往会下降.

因此这条曲线展示了“找全”和“找准”之间的权衡.

=== 评测活动

若干信息检索评测活动:

- TREC: 美国, 1992 年至今每年举办.
- NTCIR: 日本, 1997 年至今每年举办.
- 863 评测竞赛: 中国, 2005 年举办过.

其中 TREC(The Text REtrieval Conference)由美国 NIST 和 DARPA 联合举办,  
可以看作信息检索研究领域的重要公共评测平台.

TREC 的运行方式大致是:

- 年初确定任务.
- 参加者报名.
- 参加者运行系统并提交结果.
- 组织方评估结果.
- 年末召开会议交流.

早期 TREC 主要面向文本检索,  
后来逐渐扩展到语音、图像、视频、问答、跨语言检索、过滤、Web 检索等任务.

公共评测的意义在于:

- 提供统一数据集.
- 提供统一评价指标.
- 让不同系统能够公平比较.
- 推动领域内可复现研究.

== 问答系统概述

=== 问答系统与对话系统

特别提醒: 问答系统和对话系统不是同一个概念.

- 对话系统旨在模拟人与人之间的交流.
- 问答系统旨在提供更自然、更友好的信息检索工具.

问答系统更强调客观答案及其依据.  
对话系统则可以包含闲聊、主观表达、情绪回应和更开放的交互.

=== 定义

问答系统(question answering system, QA system)可以定义为:

允许用户以自然语言方式询问,  
系统能从大量异构数据中查找并总结出用户问题答案的新型信息检索系统.

例如:

```text
世界上最大的宫殿是什么宫殿?
```

系统应返回:

```text
紫禁城/故宫
```

与普通信息检索不同, 问答系统通常不满足于返回一组文档链接,  
而是希望直接返回答案, 并最好给出证据来源.

=== 问答系统类型

几类典型问答系统.

1. 基于大规模真实文本的问答系统

这类系统从预先建立的大规模真实文本语料库中找答案.  
优点是数据相对可控, 证据来源明确.  
不足是覆盖度受语料库范围限制.

2. 基于 Web 的问答系统

这类系统从网络中寻找答案.  
它的覆盖度更高, 能利用最新网页信息.  
但网络内容质量参差不齐, 噪声和可信度问题更突出.

社区问答系统也可以看成 Web 问答的一种发展形式.  
它利用用户积累的问题和答案, 但答案质量同样需要筛选.

3. 基于知识库的问答系统

这类系统从预先构建的结构化知识库中找答案.  
优点是可以进行推理, 答案结构清晰.  
难点在于知识库构建和维护成本高, 覆盖范围有限.

4. 基于语言模型的理解式问答系统

这类系统以语言模型为底层基础,  
通过大量“问题-答案”学习和上下文理解生成答案.  
它具有较强泛化能力, 技术上也更复杂.

现代问答系统通常会把检索和生成结合起来:  
先从文档、网页或知识库中检索相关证据, 再由模型抽取、归纳或生成答案.

=== 系统基本框架

一个典型问答系统包括:

- 用户端.
- 自然语言问句.
- 问句分析.
- 查询与推理.
- 语料库、网络或知识库.
- 文本、段落或概念集合.
- 答案抽取.
- 最终答案.

其中问句分析要识别:

- 查询项.
- 答案类型.
- 问题意图.

例如“谁”“什么时候”“在哪里”“多少”等问题词,  
可以帮助系统判断答案类型是人物、时间、地点还是数量.

查询与推理负责从数据源中定位候选证据,  
答案抽取负责从候选证据中找出最可能的短答案或生成最终回答.

=== QA 系统评测

TREC 竞赛 QA 任务中常见指标包括:

1. MRR

MRR(mean reciprocal rank)关注第一个正确答案出现的位置:
$
  "MRR" = 1/N sum_(i=1)^N 1/"rank"_i
$
其中$"rank"_i$表示第$i$个问题的第一个正确答案排名.  
如果某问题没有正确答案, 该项通常记为$0$.

MRR 越高, 说明系统越能把正确答案排在前面.

2. CWS

CWS(confidence weighted score)考虑系统对答案的把握程度.  
计算前, 系统应按答案置信度从高到低对问题排序.  
它强调系统不仅要答对, 还要知道自己什么时候更有把握.

3. Accuracy

准确率定义为:
$
  "Accuracy"
  = ("答案正确的问题数") / ("全部问题数")
$

Accuracy 简单直观, 但忽略了答案排序和系统置信度.

== 本章小结

本章主线可以概括为:

1. 信息检索要解决的是从大规模文本集合中找出满足用户信息需求的资料.
2. 一个基本检索系统通常包括文本预处理、文本标引、相关度评估和排序返回.
3. 布尔模型强调规则匹配, 简单高效, 但缺少部分匹配和排序能力.
4. 向量空间模型把文档和查询表示为词项权重向量, 可用余弦相似度排序.
5. 基于语言模型的检索把查询看作由文档语言模型生成的样本, 按生成概率排序.
6. 概率模型直接估计文档与查询相关的概率.
7. LSI 通过 SVD 分解词项-文档矩阵, 引入潜在语义维度, 缓解同义词和多义词问题.
8. 检索系统评测关注排序结果质量, 常用`P@N`、R-Precision、Precision-Recall 曲线等指标.
9. 问答系统可以看作信息检索的进一步发展, 目标是直接返回自然语言问题的答案.

因此, 信息检索可以看成 NLP 中连接“文本资源”和“用户需求”的关键技术.  
它既依赖语言处理, 也依赖统计建模、线性代数、概率推断和系统工程.
